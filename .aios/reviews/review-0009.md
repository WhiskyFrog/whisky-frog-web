---
schema: aios.review/v1
id: review-0009
project: whisky-frog-web
task: task-0007
attempt: 2
verdict: pass
---

# Review of task-0007, Attempt 2

## Findings

All Acceptance Criteria are met and Attempt 2's claims check out against the actual repository state. Independently re-ran the safe, non-server commands: `npm test` → 117/117 passing, `npx tsc --noEmit` (part of npm test) clean, `npm run build` → succeeds with all 12 routes. Confirmed via `ps aux` that no next-server/playwright process is currently running (consistent with the claim that Playwright's webServer is a foreground child torn down on exit). Verified tests/request-parity.test.mts (191 lines) contains all 12 required cases (default, multi-select, single-select, zero/false, ranges, dependency parents, selected-irrelevant, peated unknown, edition state, search, availability, paginated) for both catalog and market scopes (24 tests) plus the paginated-parameters test and the separate count_unit=product-vs-offer assertion (26 total, matching the claimed 91+26=117). Verified scripts/prod-smoke.mts and both dated evidence reports (2026-07-22, 2026-07-23) exist and show real 13/13 passing runs against the live deployed backend with concrete operationId/schema/count detail. Verified `npx playwright test --list` enumerates 42 tests across 2 spec files matching the claimed full-matrix count. Verified all five claimed bug fixes are actually present in the code: (1) e2e/support/dom.ts's `checkControl` helper with click+retrying toBeChecked(); (2) `aria-disabled` genuinely absent from both the terms and range group sections of ProductFacetPanel.tsx (lines 92-101, 199-201), leaving only the `opacity-60` visual dimming; (3) ProductFacetSidebar.tsx now has role="dialog", aria-modal, a real focus-trap-and-restore effect, and a new `resultNoun` prop that MarketView.tsx correctly wires to "오퍼" for the legacy per-market fallback; (4) e2e/catalog.spec.ts overrides both catalogFacetsV2 and catalogFacetsLegacy totals in the pagination test; (5) e2e/support/mock-api.ts's price-history route has the described 200ms deliberate delay, and market.spec.ts's offer-total assertion is scoped to the unique '총 N개 오퍼' text. RUNBOOK.md is thorough, documents the rollout/fallback runbook and legacy-removal gate exactly as required, records the real e2e:v2 (22/22) and e2e:legacy (22/22) results plus the full-matrix 42/42 (v2) and 34-passed/8-expected-skipped (legacy) results, and correctly states step 5 (post-deployment smoke) is not claimed since no v2 release exists yet. `git status`/`git diff` confirm nothing under .aios/ was touched and the only implementation-file changes (ProductFacetPanel.tsx, ProductFacetSidebar.tsx, MarketView.tsx) are the described accessibility fixes with no weakened or deleted assertions anywhere in tests/ or e2e/. One non-blocking documentation note filed via ReportFindings: playwright.config.ts still carries Attempt 1's stale comment claiming the suite was never executed by the authoring session, which RUNBOOK.md now contradicts with the real results — worth a follow-up cleanup but not a defect in the evidence itself.
