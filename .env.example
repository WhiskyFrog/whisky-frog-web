# 프론트엔드 환경변수 템플릿
# 복사 → frontend/.env.local (로컬) / Vercel 환경변수 (운영)

# 로컬 개발: 브라우저가 백엔드에 직접 연결
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Vercel 운영: 브라우저는 same-origin 프록시를 사용하고 실제 백엔드 주소는 서버 환경변수에 둔다.
# NEXT_PUBLIC_API_BASE_URL=/backend-api
# BACKEND_API_ORIGIN=https://203.0.113.10
